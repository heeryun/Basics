#  to start working run these prompts in the terminal
# gets the latest changes
# git pull 

# while working
# saves your progress
# git add .

# when you've made a chunk of progress
# saves a snapshot 
# git commit -m 'update message here'

# when done working
# Send everything to github
# git push

#INTRODUCTION
#to run the file type [python start_here.py] into the Command Prompt in VS code

#different types of data
age = 25                  # Integer (whole number)
price = 19.99             # Float (decimal number)
name = "Alex"             # String (text)
is_active = True          # Boolean (True or False value)


# A list of items
fruits = ["apple", "banana", "cherry"]

# A dictionary storing a user's details
# {key: values}
user_profile = {"username": "coder123", "level": 5}

print("Hello World!")     # Outputs a text message

user_age = input("Enter age: ") # Captures user typing (always reads as a string)

print(user_age) 
